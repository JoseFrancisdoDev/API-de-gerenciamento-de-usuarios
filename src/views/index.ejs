<!DOCTYPE html>
<html lang="pt-br">

<%- include('./partial/head.ejs') %>

  <body>
    <%- include('./partial/navbar.ejs') %>
      <div class="container">
        <div class="content">

          <% if (users.length> 0) { %>
            <% users.forEach( user=> { %>
              <h3 class="title">
                Nome: <%= user.firstName %>
                  <%= user.lastName %>
              </h3>
              <p>
                E-mail: <%= user.email %>
              </p>
              <% }) %>
                <% } else{ %>
                  <h3>Nenhum usuário cadastrado</h3>
                  <% } %>
        </div>
      </div>
  </body>

</html>